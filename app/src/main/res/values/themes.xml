<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StockPrice" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.StockPrice" parent="Base.Theme.StockPrice" />
    <!-- Light mode -->
    <!-- Screen background if light mode on -->
    <style name="SCREEN">
        <item name="android:background">@color/white</item>
    </style>
    <!-- Texts background if light mode on -->
    <style name="TEXT">
        <item name="android:textColor">@color/black</item>
    </style>
    <!--    button background if light mode on-->
    <style name="BUTTON">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>